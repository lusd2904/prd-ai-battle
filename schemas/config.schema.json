{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://prd-ai-battle.local/schemas/config.schema.json",
  "title": "prd-ai-battle config",
  "type": "object",
  "additionalProperties": false,
  "required": ["primary", "advisors"],
  "properties": {
    "workspace": {
      "type": "string",
      "default": ".prd-ai-battle",
      "description": "Local directory for transcript, brief, matrix, and draft versions."
    },
    "offline": {
      "type": "boolean",
      "default": false,
      "description": "Use mock streaming models; no network calls."
    },
    "gateway": { "$ref": "#/$defs/gateway" },
    "primary": { "$ref": "#/$defs/model" },
    "advisors": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/model" }
    }
  },
  "$defs": {
    "gateway": {
      "type": "object",
      "additionalProperties": false,
      "description": "Shared local multi-key gateway. Models inherit base_url and api_key unless they override.",
      "properties": {
        "base_url": {
          "type": "string",
          "description": "OpenAI-compatible Chat Completions root. Default is the loopback gateway via PRD_AI_GATEWAY_URL. Do not hardcode vendor or tunnel hosts in the repo."
        },
        "api_key": {
          "type": "string",
          "description": "Gateway key, usually ${PRD_AI_GATEWAY_KEY:-}. Prefer env interpolation over plaintext."
        }
      }
    },
    "model": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "model"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Stable label shown on chat bubbles (not necessarily the vendor model id)."
        },
        "base_url": {
          "type": "string",
          "description": "Optional per-model override of gateway.base_url. Resolved from config + env. Empty means inherit the gateway."
        },
        "api_key": {
          "type": "string",
          "description": "Optional per-model key or ${ENV} interpolation. Empty means inherit gateway.api_key."
        },
        "api_key_env": {
          "type": "string",
          "description": "Optional environment variable name to read the key from (alternative to api_key)."
        },
        "model": {
          "type": "string",
          "description": "Model id sent in the Chat Completions body (whatever the gateway expects)."
        },
        "temperature": {
          "type": "number",
          "minimum": 0,
          "maximum": 2,
          "default": 0.4
        }
      }
    }
  }
}
